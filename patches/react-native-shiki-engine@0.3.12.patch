diff --git a/android/CMakeLists.txt b/android/CMakeLists.txt
index 4e4c88b..698f941 100644
--- a/android/CMakeLists.txt
+++ b/android/CMakeLists.txt
@@ -3,13 +3,16 @@ project(ReactNativeShikiEngine)
 
 set(CMAKE_VERBOSE_MAKEFILE ON)
 
-# Find prebuilt oniguruma library
+# Use the packaged Android library directly. A generic find_library cache entry
+# can resolve the host libonig when this project is included by React Native's
+# top-level autolinking build.
 find_library(LOG_LIB log)
-find_library(ONIG_LIB onig
-    PATHS ${CMAKE_CURRENT_SOURCE_DIR}/src/main/jniLibs/${ANDROID_ABI}
-    NO_CMAKE_FIND_ROOT_PATH
-    REQUIRED
-)
+set(SHIKI_ONIG_LIB "${CMAKE_CURRENT_SOURCE_DIR}/src/main/jniLibs/${ANDROID_ABI}/libonig.so")
+
+if(NOT EXISTS "${SHIKI_ONIG_LIB}")
+    message(FATAL_ERROR "Missing packaged Oniguruma library for ${ANDROID_ABI}")
+endif()
 
 # Main library
 add_library(react-native-shiki-engine SHARED
@@ -59,7 +62,7 @@ find_package(ReactAndroid REQUIRED CONFIG)
 
 # Link libraries
 target_link_libraries(react-native-shiki-engine
-    ${ONIG_LIB}
+    ${SHIKI_ONIG_LIB}
     ${LOG_LIB}
     android
     fbjni::fbjni
